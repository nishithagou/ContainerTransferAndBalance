#include <iostream>
#include "port.hpp"

using namespace std;

int main(int argc, char** argv){
    // some hardcoded values to be added
    vector<pair<Cell, Coordinate>> shipLoad;
    Cell hull(HULL);
    pair<Cell, Coordinate> hullSpot(hull, Coordinate(0, 8));
    return 0;
}