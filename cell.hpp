#ifndef CELL_H
#define CELL_H
#include "container.hpp"
enum state  {HULL, OCCUPIED, EMPTY, UNOCCUPIABLE};

class Cell{
    private:
    char state;
    Container* container;
    public:
    Cell

};
#endif